# Quick 5-minute subdomain testing configuration
# Copy this to .env for testing

# For local development (default)
# VIDEO_SERVICE_PATH=../../../reelspeed-video-service

# For subdomain deployment testing
VIDEO_SERVICE_URL=https://video.reelspeed.ai
# OR 
# VIDEO_SERVICE_PATH=https://video.reelspeed.ai

# Other required vars (use your existing .env values)
PORT=3001
NODE_ENV=development
CORS_ORIGIN=https://app.reelspeed.ai

# Database (keep your existing values)
MONGODB_URI=mongodb://localhost:27017
DB_NAME=reelspeed

# Redis (keep your existing values)  
REDIS_HOST=localhost
REDIS_PORT=6379

# Auth (keep your existing values)
JWT_SECRET=your-jwt-secret
JWT_REFRESH_SECRET=your-refresh-secret

# Storage (keep your existing values)
STORAGE_PROVIDER=cloudflare
CLOUDFLARE_R2_ACCOUNT_ID=your-account-id
CLOUDFLARE_R2_ACCESS_KEY=your-access-key
CLOUDFLARE_R2_SECRET_KEY=your-secret-key
CLOUDFLARE_R2_ENDPOINT=your-endpoint
CLOUDFLARE_R2_BUCKET=your-bucket
CLOUDFLARE_R2_PUBLIC_URL=your-public-url